example:
  organization:
    property: "I AM THE DEFAULT ORGANIZATION SERVICE"
spring:
  jpa:
    database: "POSTGRESQL"
    show-sql: "true"
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
  datasource:
    platform: "postgres"
    url: "jdbc:postgresql://database:5432/eagle_eye_local"
    username: "postgres"
    password: "p0stgr@s"
    testWileIdle: "true"
    validationQuery: "SELECT 1"
    driverClassName: "org.postgresql.Driver"